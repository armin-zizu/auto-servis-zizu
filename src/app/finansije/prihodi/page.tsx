'use client';

import React from 'react';
import Link from 'next/link';
import AppLayout from '@/components/AppLayout';
import { useFinanceSummary } from '@/lib/financeData';
const formatKm = (value: number) => `${value.toLocaleString('bs-BA', { maximumFractionDigits: 2 })} KM`;
export default function RevenuePage() { const finance = useFinanceSummary(); const rows = [{ label: 'Naplata rada prije isplate majstora', value: finance.laborRevenue }, { label: 'Isplata majstorima (10% rada)', value: finance.mechanicEarnings }, { label: 'Naplata rada nakon isplate majstora', value: finance.laborRevenue - finance.mechanicEarnings }, { label: 'Prihod od dijelova', value: finance.partsRevenue }, { label: 'Ukupni prihod', value: finance.revenue }, { label: 'Dobit', value: finance.operatingProfit }]; return <AppLayout userRole="owner" userName="Armin Mujić" userEmail="armin@autoservis.com"><div className="px-6 lg:px-8 xl:px-10 py-6 max-w-screen-2xl mx-auto space-y-6"><div><Link href="/" className="text-xs text-primary hover:underline">Dashboard</Link><h1 className="text-2xl font-semibold text-foreground mt-2">Prihodi</h1><p className="text-sm text-muted-foreground mt-1">Zatvoreni nalozi u tekućem mjesecu: {finance.closedOrders}</p></div><div className="bg-card border border-border rounded-xl shadow-card overflow-hidden"><div className="px-5 py-4 border-b border-border"><h2 className="font-semibold text-foreground">Prihod i dobit MTD</h2></div><div className="divide-y divide-border">{rows.map((row) => <div key={row.label} className="flex items-center justify-between px-5 py-4"><p className="text-sm font-medium text-foreground">{row.label}</p><p className="text-lg font-bold text-foreground tabular-nums">{formatKm(row.value)}</p></div>)}</div></div></div></AppLayout>; }
